---
name: copyright-kit
description: "通用计算机软件著作权 (CPCC) 申报套件与合规门禁专家。当用户要求【申请软著】、【软著材料】、【导出软著代码】、【软著说明书】、【CPCC申报】、【准备软著】、【软件著作权】或输入 /copyright-kit 时强制激活。负责 60 页源程序全自动脱敏清洗、两端对齐页眉排版、7 章节用户说明书生成及 CPCC 官方门禁前置审计。"
---

# Workflow: /copyright-kit (软件著作权申报全自动化工作流)

> **适用场景**：为任何自研微服务、Web 平台或移动端 App 向中国版权保护中心（CPCC）申报《计算机软件著作权登记证书》。  
> **核心目标**：实现 60 秒内全自动完成代码脱敏清洗、标准 60 页（前 30 + 后 30）排版、说明书图文骨架生成与 CPCC 预审门禁 100% 达标。  
> **开源与自包含声明**：本 Skill 为 100% 自包含独立包，脚本零第三方依赖（纯 Python 标准库），支持全平台离线运行。

---

## 一、 核心法定规范与审核红线 (CPCC Review Guardrails)

根据国家版权局《计算机软件著作权登记办法》，申报材料必须 100% 满足以下法定硬指标，否则触发补正或驳回（延误 15~30 天）：

1. **软件全称命名律**：
   - 软件全称必须以法定名词结尾（如 `软件`、`系统`、`平台`、`工具`、`中间件`）；
   - 严禁裸英文名称（例如：申报全称仅写 `MyTool` 会被直接驳回，必须写为 `某某协同办公软件` 或 `某某数据管理系统`）。
2. **版本号格式律**：
   - 版本号必须以大写英文字母 `V` 开头（如 `V1.0` 或 `V1.0.0`），不能直接写 `1.0`。
3. **60 页代码密度律 (50行硬指标)**：
   - 提取程序连续的前 30 页与后 30 页（共 60 页，若全项目源码不足 60 页则提交全部）；
   - 每页有效代码行数**严格不少于 50 行**（推荐设置为 55 行，自动过滤连续空行与无效纯符号注释）；
   - 每页顶部必须具备统一格式的两端对齐页眉（左侧为 `软件全称 + 版本号`，右侧为 `第 X 页`，下方横线分隔）。
4. **敏感词与权属零容忍**：
   - 代码中严禁出现 `TODO`、`FIXME`、`XXX`（审查员会判定为未开发完成）；
   - 严禁出现第三方开源商业版权声明（如 `Copyright (c) 20xx Facebook/Google...`），权属必须 100% 归属于申报主体。
5. **材料一致性契约**：
   - 申请表、源程序页眉、使用说明书封面与标题中的软件全称和版本号必须 **100% 字符串一致**。

---

## 二、 自动化申报工作流 (5 步标准闭环)

### 步骤 0：申报主体性质决策 (Gate 0: 个人 vs 企业法人)
在生成任何材料前，必须反向引导用户选择申报主体性质，并说明对后续业务的关键影响：

| 决策维度 | 选项 A：【个人（自然人）】申报 | 选项 B：【公司（企业法人）】申报 (推荐用于商用App) |
| :--- | :--- | :--- |
| **所需资质** | 仅需**个人身份证正反面彩色扫描件** | 需**营业执照副本彩色扫描件**、**企业公章**、法人身份证 |
| **签章流程** | **免公章**，申请表签章页仅需个人手写签字 | 申请表签章页必须**加盖公章并由法人签字** |
| **应用市场上架** | **严重受限 ❌**（华为/小米/OPPO等各大应用商店均已限制个人开发者提交收支/记账/工具类App） | **一路绿灯 ✔**（企业开发者账号上架无阻碍，软著权属与上架主体一致） |
| **资产与高企** | 属于个人私产；如需注入公司需走繁琐的软著转让变更（耗时1~2个月） | 计入公司无形资产，可直接用于高新技术企业认定与税收优惠 |
| **保护期限** | 作者终生及死后 50 年 | 软件开发完成之日起 50 年 |

### 步骤 1：交互式信息采集 (Metadata Gathering)
明确主体性质后，采集或确认具体字段：
1. **若为企业**：企业全称（必须与营业执照公章完全一致，如 `某某科技有限责任公司`）；
2. **若为个人**：申请人真实姓名、身份证号码；
3. **软件全称与简称**：
   - 全称必须以法定名词结尾（如 `某某数字化协同管理软件`）；
   - 简称须与后续应用商店提审名称一致（如 `某某协同`，可选）。

### 步骤 2：一键抽取与排版 60 页标准源程序
调用本 Skill 内置脚本对源码进行深度清洗与分页（零第三方依赖，自适应探测）：

```bash
# 在当前待申报的项目根目录下执行 (使用 Skill 自带脚本的相对/动态路径)
python3 <path-to-skill>/scripts/export_copyright_source.py \
  --app-name "<软件全称>" \
  --version "V1.0"
```

> **注**：
> - 脚本内置源码目录自动嗅探能力（自动探测 `src/`, `app/`, `apps/`, `lib/`, `packages/` 等），无需硬编码目录；
> - 产物默认生成在当前项目的 `docs/copyright/source_code_60pages.docx` (原生排版 Word) 与 `source_code_60pages.txt`；
> - 每页 55 行纯净代码，带两端对齐页眉与标准分页符；并自动回填全项目真实有效行数 (SLOC) 至申报信息卡。

### 步骤 3：生成使用说明书骨架与填报表卡
1. 参考本 Skill 内置模板 `templates/user_manual_template.md`，结合项目实际页面与路由生成 `docs/copyright/software_user_manual.md`（包含 7 大标准章节：系统概述、安装与启动隐私、核心业务操作、辅助功能、安全注销与著作权人声明）；
2. 依据 `templates/cpcc_form_fields.md` 生成 `docs/copyright/cpcc_application_info.md`，提供可在 CPCC 网站直接复制的 200 字功能摘要与软硬件环境指标。

### 步骤 4：静态门禁前置审查 (Copyright Doctor Pre-flight)
在材料交付给人类前，强制执行预检门禁：

```bash
python3 <path-to-skill>/scripts/audit_copyright_package.py \
  --dir docs/copyright \
  --app-name "<软件全称>" \
  --version "V1.0"
```

**门禁核验 4 大通过卡点**：
- [x] 软件全称法定后缀合规 (`软件/系统/平台/工具/中间件`)；
- [x] 60 页源程序每页有效行数 `>=50` 行，总页数等于 60；
- [x] 0 TODO / 0 FIXME / 0 第三方 Copyright 敏感词；
- [x] 说明书章节完整，全称与版本号交叉核对 100% 一致。

---

## 三、 线上提报实操与转 PDF 指引

材料生成并通过审计后，向用户提供以下 4 步无纸化申报指引：

1. **源程序转 PDF**：直接使用 Word 或 WPS 双击打开自动生成的 `source_code_60pages.docx`（已内置 A4、2cm 边距、Consolas 8.5pt 与两端对齐页眉），点击 **「另存为 PDF」** 即可瞬间交付；亦可通过 `.txt` 手动排版导入；
2. **说明书贴图转 PDF**：在 `software_user_manual.md` 骨架中粘贴 5~8 张系统真实运行截图，导出为 `用户操作说明书.pdf`（约 15~20 页）；
3. **资质准备**：准备企业《营业执照副本》彩色扫描件加盖公章（或个人身份证扫描件）；
4. **CPCC 在线提报**：登录 [中国版权保护中心微平台](https://register.ccopyright.com.cn/)，对照 `cpcc_application_info.md` 逐项复制填入，上传 PDF 附件并提交签章。
